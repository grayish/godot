#!/usr/bin/env python
from misc.utility.scons_hints import *

Import("env")
Import("env_modules")

# env_modules 는 모듈 공용 빌드 환경(경고 수준 등). 복제해서 이 모듈만의 옵션을 얹는다.
env_summator = env_modules.Clone()

# 이 디렉터리의 모든 .cpp 를 엔진의 모듈 소스 목록에 추가 — 최종 링크에 정적으로 포함된다.
env_summator.add_source_files(env.modules_sources, "*.cpp")
