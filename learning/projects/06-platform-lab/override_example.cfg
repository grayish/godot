; override.cfg 예시 (실제 적용되지 않도록 이름을 override_example.cfg 로 두었다).
; 이 파일을 "override.cfg" 로 복사해 프로젝트 루트(res://) 또는 익스포트한 실행 파일 옆에 두면
; core/config/project_settings.cpp _setup() 이 project.godot 을 읽은 직후 _load_settings_text() 로 덮어쓴다.
; 포맷은 project.godot 과 같고 기능 태그 오버라이드(키.태그)도 그대로 적용된다.

[demo]

greeting="override.cfg 가 바꾼 값"
greeting.editor="override.cfg 가 바꾼 에디터 값"

[display]

window/size/viewport_width=1024
window/size/viewport_height=576

[rendering]

gl_compatibility/driver.linuxbsd="opengl3_es"
